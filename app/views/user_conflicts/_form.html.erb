<%= form_with(model: user_conflict, local: true) do |form| %>
  <% if user_conflict.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_conflict.errors.count, "error") %> prohibited this user_conflict from being saved:</h2>

      <ul>
        <% user_conflict.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_access_id %>
    <%= form.text_field :user_access_id %>
  </div>

  <div class="field">
    <%= form.label :conflict_id %>
    <%= form.text_field :conflict_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
